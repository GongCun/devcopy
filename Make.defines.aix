# Common make definitions.

CC = xlc
LDDIR = $(ROOT)/lib
LIBMYLIB = $(LDDIR)/libmylib.a
LDFLAGS = -L$(LDDIR)
LDLIBS = -lmylib -lz -lcurses
CFLAGS = -O2 -q64 $(EXTRA)
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) -c
LINK.c = $(CC) $(CFLAGS) $(LDFLAGS)
AR = ar -X64
RANLIB = ranlib
TEMPFILES = core core.* *.o temp.* *.out *~

